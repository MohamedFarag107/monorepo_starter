{
  "name": "@teamhr/main",
  "version": "0.0.0",
  "description": "team hr entry point",
  "main": "index.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development ts-node-dev -r tsconfig-paths/register  --respawn --transpile-only src/index.ts",
    "prod": "NODE_ENV=production ts-node-dev -r tsconfig-paths/register  --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express-mongo-sanitize": "^2.2.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/morgan": "^1.9.9"
  }
}
